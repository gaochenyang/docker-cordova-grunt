FROM jenkins
MAINTAINER jrontEnd "mingxiruliu@gmail.com"

# install Node
RUN curl https://nodejs.org/dist/v4.5.0/node-v5.5.0-linux-x64.tar.gz | tar xz -C /usr/local/ --strip=1

# install Android

# install gradle

# install cordova
